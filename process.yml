apps:
  - script   : app.js
    instances: 1
    exec_mode: cluster
    autorestart: false
    node_args: "-r esm"
    watch  : false
    env    :
      npm_package_name:  ${name}
      npm_package_version: ${version} 
